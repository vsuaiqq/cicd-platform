export const helpDocs = {
  overview: {
    title: 'Справочник .cicd.yaml',
    desc:
      'Разместите файл `.cicd.yaml` в корне репозитория, чтобы описать пайплайн. Flow читает его при срабатывании триггера (push, pull request, ручной запуск или cron) и выполняет джобы в порядке зависимостей.',
    tip:
      'Файл из репозитория можно переопределить в Настройки → Конфигурация пайплайна. YAML из интерфейса имеет приоритет. На вкладке Запуски редакторы могут запустить пайплайн вручную, перезапустить run или перезапустить только упавшие джобы.',
    minimalExample: 'Минимальный рабочий пример:',
  },
  global: {
    title: 'Глобальные поля',
    desc: 'Ключи верхнего уровня, которые применяются ко всему файлу пайплайна.',
    fields: {
      name: 'Человекочитаемое имя пайплайна. Отображается в деталях запуска и логах.',
      on: 'Конфигурация триггеров. См. раздел on (триггеры).',
      env:
        'Пары ключ/значение как переменные окружения во все джобы и шаги. Наименьший приоритет внутри `.cicd.yaml` — переопределяется `env` джоба, `env` шага, затем секретами.',
      environments:
        'Именованные окружения деплоя, на которые ссылаются джобы через `environment:`. Каждое может задавать URL и правила защиты. См. раздел environments.',
      jobs: 'Словарь джобов. Ключи — внутренние ID (буквы, цифры, дефисы).',
    },
  },
  on: {
    title: 'on — триггеры',
    desc:
      'Определяет, когда запускается пайплайн. Можно комбинировать `push`, `pull_request`, `manual` и `schedule` — каждое подходящее событие создаёт новый run.',
    fields: {
      push:
        'Запуск по push-webhook. Фильтры: `branches`, `paths`, `paths_ignore`. Path-фильтры смотрят на файлы в push-коммитах.',
      pull_request:
        'Запуск по pull-request webhook. Фильтры: целевые `branches`, `types`, `drafts`, path-фильтры по изменённым файлам.',
      manual:
        'Входные параметры для ручного запуска с вкладки Запуски. Каждый input доступен шагам как `MANUAL_INPUT_<NAME>` (uppercase).',
      schedule:
        'Запуск по cron. Каждый элемент списка: 5-полевое выражение `cron` и опционально `branch` / `timezone` (по умолчанию UTC).',
      branches:
        'Glob-шаблоны веток (`main`, `release/*`). Для `push` — исходная ветка; для `pull_request` — целевая (base). Не указано = любая ветка.',
      paths:
        'Список glob (`**`, `*`, ведущий `!` для исключения). Run только если хотя бы один изменённый файл совпал после исключений.',
      paths_ignore:
        'Обратный фильтр: пропустить run, если все изменённые файлы попали под эти glob (например, только docs).',
      types:
        'Типы PR-событий: `opened`, `synchronize`, `reopened` (по умолчанию, если не указано).',
      drafts:
        'При `false` (по умолчанию) draft PR пропускаются. Определение draft на GitHub может быть ненадёжным в зависимости от git-хоста.',
      inputs:
        'В `manual.inputs.<name>`: `description`, `default`, `required` (только подсказка в UI).',
      cron: 'Стандартный 5-полевой cron: `minute hour day-of-month month day-of-week`.',
      timezone: 'IANA timezone для cron (например `Europe/Amsterdam`). По умолчанию: `UTC`.',
    },
    pathTip:
      'Path-фильтры на `push` используют списки файлов из webhook. Для `pull_request` фильтрация работает только если git-хост передаёт метаданные об изменённых файлах.',
  },
  jobs: {
    title: 'jobs',
    desc:
      'Карта определений джобов. Ключ — уникальный ID для `needs`. Джобы без зависимостей идут параллельно; с `needs` — после завершения указанных (success или skipped). Matrix-джоб разветвляется на параллельные варианты — см. раздел matrix.',
    tip:
      'Джобы без общих связей `needs` выполняются параллельно. Используйте `needs` для fan-in перед финальной стадией.',
  },
  jobFields: {
    title: 'Поля джоба',
    desc: 'Поля внутри каждого определения джоба.',
    fields: {
      name: 'Отображаемое имя в UI. Если не указано — используется ID джоба.',
      image:
        'Docker-образ (`golang:1.22`, `node:20`, …). Необязательно — без образа шаги выполняются на хосте раннера (без Docker и без cache bind-mount).',
      needs:
        'ID джобов, которые должны завершиться до старта. Циклические зависимости отклоняются при разборе.',
      env:
        'Переменные окружения джоба. Поддерживает `${{ secrets.NAME }}`, `${{ matrix.KEY }}`, `${{ branch }}`, `${{ sha }}`.',
      timeout:
        'Максимальное время джоба. Строки длительности Go: `5m`, `1h30m`. По умолчанию — без лимита.',
      retry:
        'Дополнительные попытки при падении. Краткая форма `retry: 2` или `retry: { max: 2 }` — до 3 попыток всего.',
      allow_failure:
        'При `true` падение джоба не валит весь пайплайн. Зависимые джобы с `needs` всё равно запускаются.',
      manual:
        'При `true` джоб ждёт ручного подтверждения. Эквивалент `approval: required`.',
      approval:
        'Алиас для `manual`. Принимает `required`, `true`, `yes` или `1`.',
      strategy:
        'Matrix-расширение — см. раздел matrix. Создаёт параллельные варианты с env `MATRIX_<KEY>`.',
      if:
        'Булево выражение — при false джоб пропускается (зависимые считают его успешным). Вычисляется при dispatch после зависимостей. См. conditions.',
      environment:
        'Имя из `environments`. Применяет правила защиты и задаёт `DEPLOY_ENV` / `DEPLOY_URL`.',
      cache: 'Кэш между запусками (только Docker-джобы). См. раздел cache.',
      artifacts: 'Upload путей и/или download артефактов из зависимых джобов. См. artifacts.',
      steps: 'Упорядоченный список шагов.',
    },
  },
  steps: {
    title: 'steps',
    desc:
      'Упорядоченный список шагов в джобе. Выполняются последовательно в одном контейнере (или на хосте без `image`). Падающий шаг прерывает следующие, если не задан `continue_on_error`.',
    fields: {
      name: 'Метка шага в UI и логах.',
      run:
        'Shell-скрипт. Многострочные — блочный литерал YAML (`|`). По умолчанию `/bin/sh -e`.',
      env: 'Переменные только для этого шага.',
      timeout: 'Лимит времени шага.',
      retry: 'Дополнительные попытки шага.',
      continue_on_error:
        'При `true` джоб продолжается после ненулевого exit code шага.',
      if:
        'Булево выражение — при false шаг помечается skipped и не выполняется. Тот же синтаксис, что у job `if:`, вычисляется при dispatch. См. conditions.',
    },
  },
  matrix: {
    title: 'strategy — matrix',
    desc:
      'Разворачивает одно определение джоба в несколько параллельных вариантов. Каждая комбинация получает env `MATRIX_<KEY>=<value>` и подстановку `${{ matrix.<key> }}` в строках.',
    fields: {
      matrix:
        'Карта имён осей к спискам значений. Декартово произведение задаёт варианты.',
      include:
        'Дополнительные явные комбинации поверх произведения.',
      exclude:
        'Комбинации, исключаемые из произведения.',
      fail_fast:
        'При `true` первый упавший вариант отменяет оставшиеся siblings в той же matrix-группе.',
    },
    tip:
      'ID matrix-джобов в UI выглядят как `<jobId> (<value1>, <value2>, …)`. В `needs` указывайте родительский ID — оркестратор ждёт все варианты.',
  },
  conditions: {
    title: 'if — условия',
    desc:
      'Поля `if:` у джобов и шагов принимают булевы выражения. При false job/step помечается `skipped`. Выражения вычисляются при dispatch — нельзя смотреть stdout или exit code предыдущих шагов того же джоба.',
    fields: {
      literals: 'Строковые и булевы литералы в одинарных кавычках: `branch == \'main\'`, `event != \'pull_request\'`.',
      context:
        'Контекст: `branch`, `sha`, `event` (`push`, `pull_request`, `manual`, `schedule`), `env.NAME`, `matrix.NAME`, `needs.<jobId>.status`, `needs.<jobId>.result`.',
      functions:
        'Хелперы: `success()`, `failure()`, `cancelled()`, `always()`. Операторы: `==`, `!=`, `&&`, `||`, `!`.',
    },
    warn:
      'Так как `if:` вычисляется при dispatch, `success()` на шаге внутри того же джоба не отражает реальный исход шагов. Для gating по upstream используйте job `if:` с `needs.<job>.result`.',
  },
  environments: {
    title: 'environments',
    desc:
      'Верхнеуровневый `environments` задаёт цели деплоя. Джобы подключаются через `environment: <name>`. Правила защиты суммируются — все должны пройти до dispatch.',
    fields: {
      url:
        'URL деплоя. Автоматически попадает в `DEPLOY_URL`; `DEPLOY_ENV` — имя окружения.',
      branches:
        'Glob исходных веток, с которых разрешён деплой. Пусто = любая ветка.',
      wait_timer:
        'Минимальная задержка после готовности джоба (например `5m`, `30s`).',
      required_reviewers:
        'User ID, которым разрешено approve. При непустом списке — пауза awaiting_approval как при `manual: true`.',
      reviewer_count:
        'Парсится из YAML; сейчас принудительно требуется один approver из списка (N-of-M ещё не реализован).',
    },
    tip:
      'Защита окружения складывается с `manual:` / `approval:` джоба.',
  },
  cache: {
    title: 'cache',
    desc:
      'Кэширует каталоги между запусками по ключу. При hit пути восстанавливаются до шагов; при miss — сохраняются после. Кэш требует Docker `image` — host-джобы игнорируют `cache`.',
    fields: {
      key:
        'Ключ кэша. Поддерживает `${{ checksum "file" }}`, `${{ branch }}`, `${{ matrix.KEY }}`.',
      paths:
        'Пути внутри контейнера. Относительные — от `/workspace`.',
    },
    tip:
      'Ключи общие для веток на одном раннере. Добавляйте квалификатор ветки или matrix при необходимости изоляции.',
  },
  artifacts: {
    title: 'artifacts',
    desc:
      'Файлы джоба (`paths`) архивируются после шагов. Другие джобы того же run могут забрать их через `download` до своих шагов. Артефакты только в рамках одного run — межrun-хранилища и кнопки скачивания в UI пока нет.',
    fields: {
      paths: 'Glob или пути для архивации после успешного джоба.',
      download:
        'Список `{ job: <jobId> }`. Артефакты этих джобов распаковываются в workspace до шагов.',
    },
  },
  expressions: {
    title: 'Шаблонные выражения',
    desc:
      'Подстановка `${{ ... }}` в env, ключах кэша, именах образов и командах шагов.',
    fields: {
      checksum:
        'SHA-256 файла (hex). Вычисляется на раннере из checkout.',
      branch: 'Ветка, запустившая run.',
      sha: 'Полный SHA коммита.',
      secrets:
        'Секрет проекта `NAME`. Только в значениях `env`. Маскируется в логах.',
      matrix:
        'Значение оси matrix для текущего варианта: `${{ matrix.go }}`, `${{ matrix.os }}`.',
    },
  },
  secrets: {
    title: 'Секреты и переменные окружения',
    desc: 'Приоритет переменных окружения (выше — важнее):',
    priorityList: [
      'Секреты — Настройки → Секреты. Наивысший приоритет, маскируются в логах.',
      'Step env — `env` внутри шага.',
      'Job env — `env` в джобе.',
      'Pipeline env — верхний `env` в `.cicd.yaml`.',
      'Project env — Настройки → Переменные окружения.',
      'Manual inputs — `on.manual.inputs` как `MANUAL_INPUT_<NAME>` (uppercase) при ручном запуске.',
      'Environment — `DEPLOY_ENV` и `DEPLOY_URL` при `environment:`.',
    ],
    warn:
      'Секреты только для записи после сохранения. Ссылайтесь через `${{ secrets.NAME }}` в `env` или `$NAME` в shell.',
  },
  limitations: {
    title: 'Известные ограничения',
    desc: 'Текущее поведение, которое важно учитывать при написании пайплайнов:',
    items: [
      'Job/step `if:` вычисляется при dispatch, а не после каждого шага внутри джоба.',
      'Кэш работает только при указанном Docker `image`. Host-джобы кэш не используют.',
      'Артефакты доступны только в рамках одного run (через `artifacts.download`).',
      'Live-логи есть на backend; UI показывает вывод шагов после завершения джоба.',
      'PR path-фильтры зависят от метаданных git-хоста — не все хосты передают списки файлов.',
      'Определение draft PR на GitHub может возвращать `draft: false` из-за ограничений API.',
      '`reviewer_count` парсится, но сейчас принудительно один approver из `required_reviewers`.',
      'Поле `required` у manual inputs проверяется только в UI, не на сервере.',
    ],
  },
  example: {
    title: 'Полный пример',
    desc:
      'Go-проект с push/PR/manual/schedule, matrix, кэшем, артефактами, защитой окружения и условным деплоем. См. также `test-repo/.cicd.yaml` в репозитории платформы.',
  },
} as const
